import tokenTypes from "./token-types";

export default {
    a: tokenTypes.INDEFINITE_ARTICLE,
    an: tokenTypes.INDEFINITE_ARTICLE,
    is: tokenTypes.IS,
    string: tokenTypes.TEXT,
    text: tokenTypes.TEXT,
    write: tokenTypes.WRITE
}
